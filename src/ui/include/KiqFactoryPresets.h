#pragma once

#include "KickPresetIO.h"

#include <vector>

namespace KickDrum::UI {

/** Small, useful factory set that demonstrates the physical model. */
const std::vector<KickPresetDocument>& factoryPresets();

} // namespace KickDrum::UI
